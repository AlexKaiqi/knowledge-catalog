# Catalog composition

定位：整理稿。能力简介，正文后续梳理。

承认哪些仓、Workspace 配方，以及一次命令冻结 `{repo → commit}`。组合解析发生在 Access 冻结 pin 时，不是与 Snapshot Store 并列的另一种 Store。

**独立验收。** 只测登记 / resolve / pin，不解析 frontmatter。

**不依赖也能说清的失败。** Catalog 看见 `object_id`；pin 跟着 HEAD 走。

**明确不是。** 发权、SEARCH、FUSE。
