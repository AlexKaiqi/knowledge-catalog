# Merge gates

定位：整理稿。能力简介，正文后续梳理。落在核心架构第 1 层的治理侧。

这份 Preview 有没有发生治理跃迁所需的证据。merge 时只检查已绑定清单，不临时外呼。

**独立验收。** Preview 绑定的证据清单；不在 merge 时外呼。

**不依赖也能说清的失败。** 只认分支名；Gate 当 Hook。

**明确不是。** 出站副作用、allow 表。
