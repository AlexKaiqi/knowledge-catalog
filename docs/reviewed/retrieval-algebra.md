# Retrieval algebra

定位：整理稿。能力简介，正文后续梳理。

MATCH + 类型过滤定位 CandidateRef，再在同一 basis hydrate。它是查询代数，不是索引写入，也不是交付遮罩。

**独立验收。** 代数与完整性：Exact / Superset、exhausted、无 READY 失败关闭。

**不依赖也能说清的失败。** 扫权威当搜索；把 `_source` 当正文。

**明确不是。** 谁写索引、Binding 怎么观察、交付遮罩。
