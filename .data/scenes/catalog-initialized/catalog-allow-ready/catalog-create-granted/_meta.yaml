layer: app
role: provider
surface: both
publishes:
  - catalog.allow
summary: catalog.repositories.create 是建仓准入；不放行库存发现、发权或自选存储
evidence:
  - source: catalog.allow
    tests:
      - TestManagedRepositoryProviderCreatesPublishesAndResumes
      - TestManagedRepositoryCreatePublicArgv
probes:
  - file: probe-inventory-not-implied.feature
    source: catalog.allow
  - file: probe-not-admin.feature
    source: catalog.allow

