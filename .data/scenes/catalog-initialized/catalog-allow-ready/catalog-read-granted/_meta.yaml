layer: app
role: consumer
surface: both
publishes:
  - catalog.allow
summary: catalog.read 可见本 Catalog 库存身份；不放行审计或发权
evidence:
  - source: catalog.allow
    tests:
      - TestCatalogIsolationDoesNotShareAllow
probes:
  - file: probe-audit-not-implied.feature
    source: catalog.allow
  - file: probe-not-admin.feature
    source: catalog.allow
