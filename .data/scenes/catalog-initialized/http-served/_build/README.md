# http-served

`kc serve` 已起。产品命令经 Server；`login` / `logout` / `whoami` 在此配对身份。库存 JSON 不含宿主路径。

构建与探：`TestRemoteProviderReadBackAndConsumerDiscovery`、`TestFormalServiceNamespacesAreExplicitAndRetiredRoutesStayMissing`、`TestClientCredentialCommandsLoginAndLogout`。
