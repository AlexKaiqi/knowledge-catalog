# access-audited

访问账已按时间窗 / repository / principal 可查：`operations audit access|trace|hitmap`。这不是调试日志，也不把 principal 当 metric label。

构建与探：`TestAgentDelegatedAccessTraceFeedbackAndHitmap`、`TestHTTPAccessLogQueryFiltersAndPages`。
