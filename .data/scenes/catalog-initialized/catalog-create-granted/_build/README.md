# catalog-create-granted

已认证主体持有该 Catalog 的 `catalog.repositories.create`：可以按平台供给策略申请托管仓。不放行 `catalog.read` 库存、发权、归档，也不能自选存储地址。

本机 Home 无 Deployment，产品 `kc create` 也不接受场景执行器注入的 `--home`。真正建仓走 父节点 Go evidence 中的正式配置旅程。本节点只冻结准入授权世界，并用 `grant list` 求值证明规则命中。

独立 Go 证据（使用自己的 setup，不消费本节点 fixture）：`TestManagedRepositoryProviderCreatesPublishesAndResumes`、`TestManagedRepositoryCreatePublicArgv`。
