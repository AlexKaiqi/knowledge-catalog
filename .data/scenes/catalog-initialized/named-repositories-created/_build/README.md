# named-repositories-created

已初始化部署上，接入方显式 `kc create --name` 两个 LakeFS 托管仓，仓 ID 与 Graveler 名相同，再 `attach` 进当前 Catalog。

这不是 `repository-attached`（既有仓只读登记），也不是 根节点 evidence 中的 Dolt/Gitea Go 旅程。本节点不写知识、不起接入方容器。
