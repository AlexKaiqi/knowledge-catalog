layer: app
role: operator
surface: go-test
publishes:
  - deployment.recovery
summary: 替换实例缓存后恢复既有部署；缺耐久状态失败关闭
evidence:
  - source: deployment.recovery
    tests:
      - TestDeploymentSurvivesInstanceReplacement
      - TestDeploymentMissingDurableStateFailsClosed
