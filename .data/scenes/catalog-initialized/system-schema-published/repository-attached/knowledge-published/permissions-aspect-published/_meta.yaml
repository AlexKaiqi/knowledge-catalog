layer: "2"
role: provider
surface: both
publishes:
  - snapshot.product
summary: permissions Aspect 是 SOURCE 知识，不是 kc 读闸门，也不进 allow.json
evidence:
  - source: catalog.allow
    tests:
      - TestUserJourneyKnowledgeGrantDoesNotAuthorizeAccess
probes:
  - file: probe-not-a-gate.feature
    source: catalog.allow
