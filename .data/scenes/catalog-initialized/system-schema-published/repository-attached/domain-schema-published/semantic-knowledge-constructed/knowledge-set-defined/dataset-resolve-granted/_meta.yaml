layer: app
role: consumer
surface: both
publishes:
  - catalog.allow
summary: resolver 持有 dataset.resolve；读清单要 file.read，仓知识仍要 knowledge.read
evidence:
  - source: catalog.allow
    tests:
      - TestOpenedKnowledgeSetPinDoesNotMoveWithLaterCommit
      - TestKnowledgeSetAuthorizationCoverageIsHonest
      - TestDatasetFileReadDoesNotImplyRepoKnowledge
probes:
  - file: probe-pin-without-member-read.feature
    source: catalog.allow
