layer: app
role: consumer
surface: go-test
publishes:
  - catalog.allow
summary: resource.access 按 pin 上的声明调用墙外 runtime；KC 仓读权不能替代源系统强制
evidence:
  - source: catalog.allow
    tests:
      - TestResolveDescriptorBindingAtPinnedCommit
      - TestCatalogViewsChecksAndKnowledgeResolve
