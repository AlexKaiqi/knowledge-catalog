layer: app
role: provider
surface: both
publishes:
  - catalog.allow
summary: 接入方持有 writer.commit / writer.preview；空 allow 下 --as 写失败关闭
evidence:
  - source: catalog.allow
    tests:
      - cli/write_flow_test.go
      - TestDesiredDirCommitRequiresWriterCommit
      - TestAuditTrail
probes:
  - file: probe-foreign-principal-denied.feature
    source: catalog.allow
