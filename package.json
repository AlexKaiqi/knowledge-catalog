{
  "name": "knowledge-catalog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Knowledge Catalog Phase 0 — minimal semantic layer (TypeScript)",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.8"
  }
}
