---
object_id: schema/metric.definition
kind: Entity
---
entity: Metric
aspect: definition
pattern: record
fields:
  name:
    type: string
    required: true
    access: [text, filter]
  expression:
    type: string
    required: true
    access: [text]
  semanticModelRef:
    type: object_ref
    required: true
    refTypes: [SemanticModel]
  unit:
    type: string
    access: [filter]
