// Package reader interprets layer ② Knowledge at fixed Snapshot commits.
// It owns exact reads, assembly, history, provenance and Binding resolution;
// logical search contracts live in package retrieval.
package reader
