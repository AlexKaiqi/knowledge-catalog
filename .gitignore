node_modules/
dist/
*.log
.DS_Store
.venv/
__pycache__/
*.py[cod]

# Local dumps remain ignored. Tracked under .data: warehouse black-box suite
# and protocol journey scenes (nested state tree). Each has its own gitignore
# for generated runs where needed.
.data/*
!.data/data-warehouse/
!.data/scenes/
.session_tmps/

# Archify visual-review sidecars are local QA evidence, not source artifacts.
docs/*.visual-check.*.png
docs/*.visual-check.html
docs/*.visual-check.json

# CLI workspace (FileGit repos + Catalog/Writer state)
.kc/
.kc-home/

# Go build artifacts
/kc
*.test
